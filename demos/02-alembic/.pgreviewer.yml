# pgReviewer config — demo: 02-alembic
#
# This file is loaded automatically when pgr diff is run from this directory.

rules:
  add_foreign_key_without_index:
    enabled: true
    severity: critical
  create_index_not_concurrently:
    enabled: true
    severity: warning
