# pgReviewer config — demo: 01-pure-sql
#
# This file is loaded automatically when pgr diff is run from this directory.
# All detectors are enabled at their default severity levels.

rules:
  add_foreign_key_without_index:
    enabled: true
    severity: critical
